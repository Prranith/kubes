minikube delete --all --purge
minikube start --driver=docker

docker pull nginx
minikube image load nginx

kubectl create deployment demokube --image=nginx
kubectl get deployments
kubectl get pods

kubectl scale deployment demokube --replicas=4
kubectl get pods

kubectl expose deployment demokube --type=NodePort --port=80
kubectl get svc demokube

minikube service demokube --url

kubectl delete service demokube
kubectl delete deployment demokube

minikube stop
minikube delete --all --purge

-----------------------
groovy script pipeline
-----------------------
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Building the project..."
            }
        }

        stage('Test') {
            steps {
                echo "Running tests..."
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploying..."
            }
        }
    }
}

-----------------
nagios
-------------

docker pull jasonrivers/nagios:latest
docker run --name nagiosdemo -p 8888:80 jasonrivers/nagios:latest
open browser and enter localhost://8888
Username: nagiosadmin
password: nagios

docker stop nagiosdemo
docker rm nagiosdemo



git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin ___________
git push -u origin main



--------------------
AWS - Maven_web
--------------------


ssh keyyyyyy
docker --version git --version
if docker,git not installed sudo su and sudo apt-get update

install docker cammand --- sudo apt-get install docker.io       maven---- sudo apt-get install -y maven
git clone https://github.com/Gayathri2608-hub/maven_web.git
ls
cd maven_web 
mvn clean package
ls target/
sudo docker build -t dep1 .           name of image:img1
sudo docker run -d -p 8081:8080 dep1
sudo docker images
Go to “Inbound rules” → Edit inbound rules

Add this rule:

Type	Protocol	Port Range	Source
Custom TCP	TCP	8081	0.0.0.0/0
Custom TCP	TCP	8081	::/0
http://public ipv4:8081

-------
Maven java Jenkins
-------
maven_java git branch
mvn clean install
**/* maven_java_test

maven_java_test delete workspace
maven_java **/*  mvn test
**/*




**/*.war
webapp
http://localhost:8080//
